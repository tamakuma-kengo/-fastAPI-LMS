
version: '3'
 
services: 
    demo-app-front:
        container_name: demo-app-front
        build: ./docker
        ports: 
            - 8080:8080
        volumes: 
            - ./server:/app:cached
        tty: true
        stdin_open: true
        privileged: true
        command: /bin/sh